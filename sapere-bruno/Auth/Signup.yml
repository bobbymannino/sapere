info:
  name: Signup
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/auth/signup/email"
  body:
    type: json
    data: |-
      {
        "email": "manninobobby@icloud.com",
        "username": "manninobobby",
        "password": "password"
      }
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        if (res.status >= 200 && res.status < 300) {
          const body = res.getBody(); 
          if (body?.token) {
            bru.setVar("token", body.token);
          }
        }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
