info:
  name: Login (username)
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/auth/login/email"
  body:
    type: json
    data: |-
      {
        "username": "manninobobby",
        "password": "password"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
