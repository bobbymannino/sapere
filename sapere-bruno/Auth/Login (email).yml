info:
  name: Login (email)
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/auth/login/email"
  body:
    type: json
    data: |-
      {
        "email": "manninobobby@icloud.com",
        "password": "password"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
