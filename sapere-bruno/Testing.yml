info:
  name: Testing
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/auth/signup/email"
  body:
    type: json
    data: |-
      {
        "email": "manninobobby@icloud.com",
        "username": "manninobobby",
        "password": "password"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
